import java.util.Comparator;

public class SortingClass implements Comparator<Student>
	{
		@Override
		public int compare(Student s1, Student s2)
			{
				return 0;
						//s1.getLastName().compareTo(s2.getLastName());
				//Compares students classes
			
			}
	}
